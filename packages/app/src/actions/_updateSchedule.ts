import Batch from "@/model/batch";

// TODO: stubbed — will derive the brew schedule from the batch's ingredients
export default function _updateSchedule(batch: Partial<Batch>): Partial<Batch> {
    return batch;
}
